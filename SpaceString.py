from System.Collections.Generic import *

class SpaceString(object):
	def __init__(self):
		self._s_cache = Dictionary[int, str]()

	def OfLength(len):
		if self._s_cache.TryGetValue(len, ):
			cachedValue
		return self._s_cache[len] = System.String(' ', len)

	OfLength = staticmethod(OfLength)